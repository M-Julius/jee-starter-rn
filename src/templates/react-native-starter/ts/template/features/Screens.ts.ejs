import ScreenData from '~/app/core/class/ScreenData';
import <%= name %>ScreenProps from './ScreenProps';

const <%= name %>Screens = {
    <%= name %>: new ScreenData<any>({
      KEY: '<%= name %>',
      TITLE: '<%= name %>',
    }),
}

export default <%= name %>Screens